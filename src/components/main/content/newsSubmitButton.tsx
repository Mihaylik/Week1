import React from 'react';
import s from '../main.module.scss';

const NewsSubmitButton = () => {
    return (
        <button className={s.news_submit_button} type="submit">ПІДПИСАТИСЯ</button>
    );
};

export default NewsSubmitButton;